{
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm run build",
  "outputDirectory": "dist",
  "github": {
    "silent": true,
    "enabled": true,
    "autoJobCancelation": true
  },
  "cache": {
    "paths": ["node_modules/.pnpm", "dist"]
  }
}
