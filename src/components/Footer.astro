---
const currentYear = new Date().getFullYear();
---

<footer
  class="flex flex-col gap-12 text-center text-[color:var(--gray-400)] text-sm border-t-[color:var(--gray-800)] mt-auto px-8 py-12 border-t border-solid sm:flex-row sm:justify-between sm:px-20 sm:py-10"
>
  <div class="flex flex-col gap-2 sm:flex-row sm:gap-4 sm:flex-wrap;">
    <p>&copy; {currentYear} Long Nhat Nguyen</p>
  </div>
</footer>
